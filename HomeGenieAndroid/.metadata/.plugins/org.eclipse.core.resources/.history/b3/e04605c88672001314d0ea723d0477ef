package com.glabs.homegenie;

import java.util.ArrayList;

import android.content.Context;
import android.support.v4.app.Fragment;
import android.support.v4.app.FragmentManager;
import android.support.v4.app.FragmentPagerAdapter;

import com.glabs.homegenie.service.data.Group;
import com.viewpagerindicator.IconPagerAdapter;

class TestFragmentAdapter extends FragmentPagerAdapter implements IconPagerAdapter {
    private ArrayList<Group> _groups = new ArrayList<Group>();
    private ArrayList<Fragment> _fragments = new ArrayList<Fragment>();

    public void setGroups(Context context, ArrayList<Group> groups)
    {
    	this._groups = groups;
    	this._fragments.clear();
    	int p = 0;
    	for(Group g : groups)
    	{
    		_fragments.add(TestFragment.newInstance(context, getGroup(p++).Name));
    	}
    	notifyDataSetChanged();
    	
    }
    
    public Group getGroup(int position)
    {
    	return _groups.get(position % _groups.size());
    }
    
    public TestFragmentAdapter(FragmentManager fm) {
        super(fm);
    }

    @Override
    public Fragment getItem(int position) {
    	
        return _fragments.get(position);
    }

    @Override
    public int getCount() {
        return _groups.size();
    }

    @Override
    public CharSequence getPageTitle(int position) {
      return getGroup(position).Name;
    }

    @Override
    public int getIconResId(int index) {
      return 0; //ICONS[index % ICONS.length];
    }
    
}